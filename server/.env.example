# Choose one provider and fill credentials
# SMTP Example (recommended)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
FROM_EMAIL=hello@yourdomain.com
TO_EMAIL=you@yourdomain.com

# Or use SendGrid (optional) using API key
# SENDGRID_API_KEY=
# FROM_EMAIL=hello@yourdomain.com
# TO_EMAIL=you@yourdomain.com

PORT=3000
