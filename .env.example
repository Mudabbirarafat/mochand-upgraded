# Email Configuration
# Choose ONE of the following methods:

# Option 1: SMTP (Gmail, Outlook, custom SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Option 2: SendGrid API Key (uncomment to use)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Email Settings
FROM_EMAIL=noreply@mochand.com
TO_EMAIL=contact@mochand.com

# Server
PORT=3000
NODE_ENV=development

# Note: If neither SMTP nor SendGrid is configured, the server will use
# Nodemailer Ethereal test accounts for local development testing.
